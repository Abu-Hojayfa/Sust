# Port for local Express server
PORT=3000

# LLM Providers API Keys (one or both are required for LLM-based investigation; service defaults to rule-based fallback if empty)
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
